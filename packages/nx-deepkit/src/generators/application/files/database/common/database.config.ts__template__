import { AppModuleConfig } from "@deepkit/app";
import { t } from "@deepkit/type";

export const databaseConfig = new AppModuleConfig({
  HOST: t.string,
  NAME: t.string,
  HOST_PORT: t.number,
  USER: t.string,
  PASSWORD: t.string
})
